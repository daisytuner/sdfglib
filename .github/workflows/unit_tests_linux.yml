name: Unit Tests (Linux)

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, reopened, synchronize]
  schedule:
    - cron: "0 4 * * *"

jobs:
  cpp-tests-linux:
    runs-on:
      group: dahlia
      labels: RTX5060
    container:
      image: daisytuner/docc-run-env-llvm21-ubuntu-24.04-et:latest-amd64
      options: >-
        --cap-add=PERFMON
        --gpus=all

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.BENCHMARK_TESTS_TOKEN }}
          submodules: recursive

      - name: Mark GitHub Actions workdir as safe
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Formatting
        shell: bash
        run: |
          shopt -s globstar
          clang-format-19 -style=file --dry-run --Werror sdfg/include/**/*.h sdfg/src/**/*.cpp sdfg/tests/**/*.cpp
          clang-format-19 -style=file --dry-run --Werror opt/include/**/*.h opt/src/**/*.cpp opt/tests/**/*.cpp

      - name: Build
        run: |
          mkdir build
          cd build
          cmake \
            -G Ninja \
            -DCMAKE_C_COMPILER=clang-21 \
            -DCMAKE_CXX_COMPILER=clang++-21 \
            -DCMAKE_INSTALL_PREFIX=/usr/local \
            -DCMAKE_BUILD_TYPE=Debug \
            -DSDFG_ENABLE_COVERAGE=ON \
            -DBUILD_TESTS:BOOL=OFF \
            -DBUILD_BENCHMARKS:BOOL=OFF \
            -DBUILD_BENCHMARKS_GOOGLE:BOOL=OFF  \
            -DDOCC_BUILD_TARGET_ET=ON \
            -DCMAKE_PREFIX_PATH=/opt/et \
            ..
          ninja -j$(nproc)
          ninja install

      - name: Unit Tests
        run: |
          cd build/
          LLVM_PROFILE_FILE="sdfglib_test.profraw" ./sdfg/tests/sdfglib_test
          LLVM_PROFILE_FILE="sdfgopt_test.profraw" ./opt/tests/sdfgopt_test
          LLVM_PROFILE_FILE="docc-target-et_test.profraw" ./targets/et/tests/docc-target-et_test
          LLVM_PROFILE_FILE="c-compile_test.profraw" ./c-compile/tests/c-compile_test
          ./tutorial/printf_target/tests/printf_target_test

          # Run coverage
          llvm-profdata-21 merge -sparse sdfglib_test.profraw sdfgopt_test.profraw docc-target-et_test.profraw  c-compile_test.profraw -o default.profdata
          cd ../
          llvm-cov-21 export ./build/sdfg/tests/sdfglib_test -object ./build/opt/tests/sdfgopt_test -object ./build/targets/et/tests/docc-target-et_test -object ./build/c-compile/tests/c-compile_test -instr-profile=build/default.profdata -format=lcov > build/coverage_full.lcov
          lcov --ignore-errors unused --extract build/coverage_full.lcov '*/sdfg/src/*' '*/sdfg/include/*' '*/opt/src/*' '*/opt/include/*' '*/targets/et/src/*' '*/targets/et/include/*' '*/c-compile/src/*' '*/c-compile/include/*' --output-file build/coverage.lcov

      - name: Test Arg-Capture-IO
        run: |
          cd build
          ./arg-capture-io/tests/capture_io_test

      - name: Test RTL
        run: |
          export CPATH=/usr/local/include:$CPATH
          export LIBRARY_PATH=/usr/local/lib:$LIBRARY_PATH
          export LD_LIBRARY_PATH=/usr/local/lib:$LD_LIBRARY_PATH
          export PATH=/usr/local/bin:$PATH

          pip install pytest pytest-xdist --break-system-packages
          pip install jsonschema --break-system-packages

          cd rtl/tests
          pytest -v -s rtl_tests.py

      - name: Coveralls
        uses: coverallsapp/github-action@v2
        with:
          flag-name: sdfg
          parallel: true
          format: lcov
          file: build/coverage.lcov

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '24'

      - name: Install Node dependencies for transfer server
        working-directory: ${{ github.workspace }}/examples/transfer-server
        run: |
          if [ -f package-lock.json ] || [ -f npm-shrinkwrap.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Start transfer server and run RPC tests
        working-directory: ${{ github.workspace }}
        env:
          SDFG_TEST_SDFG_PATH: ${{ github.workspace }}/examples/input/transfertuning/matmul_sdfg.json
          SDFG_TEST_SEQUENCE_PATH: ${{ github.workspace }}/examples/input/transfertuning/matmul_sequence.json
          DOCC_ACCESS_TOKEN: ${{ secrets.DOCC_CI_TOKEN }}
        shell: bash
        run: |
          set -e

          # Start the Node transfer server in the background
          cd examples/transfer-server
          npm run dev &
          SERVER_PID=$!

          # Give the server a moment to start
          sleep 5

          # Run RPC unit tests (sdfgrpc_test links against the local transfer transform)
          cd "$GITHUB_WORKSPACE/build"
          ./rpc/tests/sdfgrpc_test

          # Shut down the server
          kill "$SERVER_PID" || true

          # Run production server transfer-tuning tests
          cd "$GITHUB_WORKSPACE/build"
          ./rpc/tests/sdfgrpc_daisytuner_rpc_test

  inference-tests-linux:
    runs-on:
      group: dahlia
      labels: rocm
    container:
      image: daisytuner/docc-run-env-llvm19-vllm-rocm-ubuntu-22.04:latest-amd64
      options: >-
        --cap-add=PERFMON
        --device /dev/kfd
        --device /dev/dri

    env:
      DOCC_INFERENCE: "ON" #skips certain tests only when inference enabled.
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.BENCHMARK_TESTS_TOKEN }}
          submodules: recursive

      - name: Create virtual environment
        run: |
          python -m venv --system-site-packages .venv
          echo "$PWD/.venv/bin" >> $GITHUB_PATH
          echo "PYTHONPATH=$PWD/python:$PWD/mlir" >> $GITHUB_ENV

      - name: Mark GitHub Actions workdir as safe
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pybind11 coverage black==25.9.0 build scikit-build-core
          pip install pytest pytest-xdist
          pip install numpy scipy ml_dtypes jsonschema

          pip install cupy-rocm-7-0

      - name: Build
        run: |
          mkdir build
          cd build
          cmake \
            -G Ninja \
            -DDOCC_CXX_COMPILER="/opt/rocm/llvm/bin/clang++" \
            -DCMAKE_INSTALL_PREFIX=/usr/local \
            -DCMAKE_BUILD_TYPE=Debug \
            -DBUILD_TESTS:BOOL=OFF \
            -DBUILD_BENCHMARKS:BOOL=OFF \
            -DBUILD_BENCHMARKS_GOOGLE:BOOL=OFF  \
            -DPYTHON_BUILD_FRONTEND=ON \
            -Dpybind11_DIR=$(python -c "import pybind11; print(pybind11.get_cmake_dir())") \
            -DMLIR_BUILD_FRONTEND=ON \
            -DMLIR_DIR=/usr/lib/llvm-19/lib/cmake/mlir \
            -DLLVM_EXTERNAL_LIT=/usr/lib/llvm-19/build/utils/lit/lit.py \
            ..
          ninja -j$(nproc)

      - name: Unit Tests
        run: |
          cd build/
          ./sdfg/tests/sdfglib_test
          ./opt/tests/sdfgopt_test
          ./c-compile/tests/c-compile_test
          ./tutorial/printf_target/tests/printf_target_test
          ./arg-capture-io/tests/capture_io_test

      - name: Python Integration Tests
        env:
          DOCC_ACCESS_TOKEN: ${{ secrets.DOCC_CI_TOKEN }}
        run: |
          export LD_LIBRARY_PATH=/usr/local/lib:$LD_LIBRARY_PATH
          python -m pytest -v python/tests -m "unmarked"

      - name: MLIR Unit & Integration Tests
        env:
          DOCC_ACCESS_TOKEN: ${{ secrets.DOCC_CI_TOKEN }}
        run: |
          rocm-smi
          pip install --no-cache-dir --no-deps torch-mlir==20260512.810 -f https://github.com/llvm/torch-mlir-release/releases/expanded_assets/dev-wheels
          cd build/
          ninja check-docc-mlir
          python -m pytest -v ../mlir/integration -m "unmarked or rocm"


  ##### FRONTENDS #####

  llvm-tests-linux:
    runs-on:
      group: dahlia
      labels: Linux
    container:
      image: daisytuner/docc-build-env-llvm21-ubuntu-24.04:latest-amd64

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.BENCHMARK_TESTS_TOKEN }}
          submodules: recursive

      - name: Mark GitHub Actions workdir as safe
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Formatting
        shell: bash
        run: |
          shopt -s globstar
          clang-format-19 -style=file --dry-run --Werror llvm/include/**/*.h llvm/src/**/*.cpp llvm/tests/**/*.cpp llvm/driver/**/*.cpp

      - name: Build
        run: |
          mkdir build
          cd build
          cmake -G Ninja \
            -DCMAKE_C_COMPILER=clang-21 \
            -DCMAKE_CXX_COMPILER=clang++-21 \
            -DCMAKE_BUILD_TYPE=Debug \
            -DSDFG_ENABLE_COVERAGE=ON \
            -DLLVM_BUILD_FRONTEND=ON \
            -DLLVM_BUILD_TESTS=ON \
            -DINSTALL_GTEST=OFF \
            -DBUILD_TESTS:BOOL=OFF \
            -DBUILD_BENCHMARKS:BOOL=OFF \
            -DBUILD_BENCHMARKS_GOOGLE:BOOL=OFF \
            ..
          ninja -j$(nproc)
          cpack -G DEB
          apt-get install -y ./docc-llvm*.deb

      - name: Unit Tests
        run: |
          cd build
          LLVM_PROFILE_FILE="docc_llvm_pass_test.profraw" ./llvm/tests/docc_llvm_pass_test

          # Run coverage
          llvm-profdata-21 merge -sparse docc_llvm_pass_test.profraw -o default.profdata
          cd ../
          llvm-cov-21 export ./build/llvm/tests/docc_llvm_pass_test -instr-profile=build/default.profdata -format=lcov > build/coverage_full.lcov
          lcov --ignore-errors unused --extract build/coverage_full.lcov '*/llvm/src/*' '*/llvm/include/*' --output-file build/coverage.lcov

      - name: Coveralls
        uses: coverallsapp/github-action@v2
        with:
          flag-name: llvm
          parallel: true
          format: lcov
          file: build/coverage.lcov

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Setup virtual environment
        run: |
          python -m venv .venv
          echo "$PWD/.venv/bin" >> $GITHUB_PATH

          python -m pip install --upgrade pip
          pip install pytest pytest-xdist lit

      - name: Integration Tests
        run: |
          export LLVM_SYMBOLIZER_PATH=$(which llvm-symbolizer-21)

          cd llvm/integration
          pytest -v llvm_test_suite.py

  mlir-tests-linux:
    runs-on:
      group: dahlia
      labels: Linux
    container:
      image: daisytuner/docc-run-env-llvm21-ubuntu-24.04:latest-amd64
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.BENCHMARK_TESTS_TOKEN }}
          submodules: recursive

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Create virtual environment
        run: |
          python -m venv .venv
          echo "$PWD/.venv/bin" >> $GITHUB_PATH
          echo "PYTHONPATH=$PWD/python:$PWD/mlir" >> $GITHUB_ENV

      - name: Mark GitHub Actions workdir as safe
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pybind11 coverage black==25.9.0 build scikit-build-core
          pip install pytest pytest-xdist
          pip install numpy scipy ml_dtypes jsonschema

      - name: Build
        run: |
          mkdir build
          cd build
          cmake -G Ninja \
            -DCMAKE_C_COMPILER=clang-21 \
            -DCMAKE_CXX_COMPILER=clang++-21 \
            -DCMAKE_BUILD_TYPE=Debug \
            -DPYTHON_BUILD_FRONTEND=ON \
            -Dpybind11_DIR=$GITHUB_WORKSPACE/.venv/lib/python${{ matrix.python-version }}/site-packages/pybind11/share/cmake/pybind11 \
            -DMLIR_BUILD_FRONTEND=ON \
            -DMLIR_DIR=/usr/lib/llvm-21/lib/cmake/mlir \
            -DLLVM_EXTERNAL_LIT=/usr/lib/llvm-21/build/utils/lit/lit.py \
            -DINSTALL_GTEST=OFF \
            -DBUILD_TESTS:BOOL=OFF \
            -DBUILD_BENCHMARKS:BOOL=OFF \
            -DBUILD_BENCHMARKS_GOOGLE:BOOL=OFF \
            ..
          ninja -j$(nproc)

      - name: Unit Tests
        run: |
          cd build/
          ninja check-docc-mlir

      - name: Integration Tests
        env:
          DOCC_ACCESS_TOKEN: ${{ secrets.DOCC_CI_TOKEN }}
        run: |
          pip install -r mlir/requirements.txt

          cd mlir/
          pytest -v -n auto integration/ -m "unmarked"

      # - name: Model Zoo Tests (Nightly only)
      #   if: github.event_name == 'schedule'
      #   run: |
      #     # Install additional dependencies for model zoo tests
      #     pip install transformers pillow requests matplotlib

      #     # Run fasterrcnn test (will fail if assertions don't pass)
      #     cd mlir/benchmarks/torch/model_zoo/
      #     python fasterrcnn_resnet50.py

  python-tests-linux:
    runs-on:
      group: dahlia
      labels: Linux
    container:
      image: daisytuner/docc-run-env-llvm21-ubuntu-24.04-et:latest-amd64
      options: >-
        --cap-add=PERFMON
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13", "3.14"]

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.BENCHMARK_TESTS_TOKEN }}
          submodules: recursive

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Create virtual environment
        run: |
          python -m venv .venv
          echo "$PWD/.venv/bin" >> $GITHUB_PATH

      - name: Mark GitHub Actions workdir as safe
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pybind11 coverage black==25.9.0 build scikit-build-core
          pip install pytest pytest-xdist
          pip install sympy numpy ml_dtypes scipy

      - name: Formatting
        shell: bash
        run: |
          shopt -s globstar
          clang-format-19 -style=file --dry-run --Werror python/bindings/**/*.h python/bindings/**/*.cpp
          black --check python/docc/ python/tests/

      - name: Build
        run: |
          # scikit sets this internally to find pybind, so cannot override via -Ccmake
          export CMAKE_PREFIX_PATH=/opt/et

          pip install --no-build-isolation \
            -Ccmake.build-type=Debug \
            -Ccmake.define.DOCC_BUILD_TARGET_ET=ON \
            -v -e python/

      - name: Unit Tests
        env:
          DOCC_ACCESS_TOKEN: ${{ secrets.DOCC_CI_TOKEN }}
        run: |
          export LD_LIBRARY_PATH=/usr/local/lib:$LD_LIBRARY_PATH

          coverage run --source=python/docc -m pytest -v -n auto python/tests -m "unmarked"
          coverage run --source=python/docc -m pytest -v -n auto python/tests -m "etsoc"
          coverage xml

      - name: Integration Tests
        run: |
          export LD_LIBRARY_PATH=/usr/local/lib:$LD_LIBRARY_PATH

          pytest -v -n auto python/benchmarks/ -k none
          pytest -v -n auto python/benchmarks/ -k sequential
          pytest -v -n auto python/benchmarks/ -k openmp

      - name: Coveralls
        uses: coverallsapp/github-action@v2
        with:
          flag-name: python-${{ matrix.python-version }}
          parallel: true
          format: cobertura
          file: coverage.xml

  pytorch-tests-linux:
    runs-on:
      group: dahlia
      labels: Linux
    container:
      image: daisytuner/docc-run-env-llvm21-ubuntu-24.04:latest-amd64
      options: >-
        --cap-add=PERFMON
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13", "3.14"]

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Create virtual environment
        run: |
          python -m venv .venv
          echo "$PWD/.venv/bin" >> $GITHUB_PATH

      - name: Mark GitHub Actions workdir as safe
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r pytorch/requirements.txt

      - name: Check Formatting & Typing
        run: |
          black --check pytorch/
          pyright -p pytorch/

      - name: Build frontends
        run: |
          pip install --no-build-isolation \
            -Ccmake.build-type=Debug \
            -v -e python/
          pip install --no-build-isolation \
            -v -e pytorch/

      - name: Unit Tests
        env:
          DOCC_ACCESS_TOKEN: ${{ secrets.DOCC_CI_TOKEN }}
        run: |
          coverage run --source=pytorch/docc -m pytest -v -n auto pytorch/tests
          coverage xml
          pytest -v -n auto pytorch/tests --target=sequential
          pytest -v -n auto pytorch/tests --target=openmp

      - name: Coveralls
        if: matrix.python-version == '3.14'
        uses: coverallsapp/github-action@v2
        with:
          flag-name: pytorch
          parallel: true
          format: cobertura
          file: coverage.xml

  ##### TARGETS #####

  cuda-tests-linux:
    runs-on:
      group: dahlia
      labels: RTX5060
    container:
      image: daisytuner/docc-run-env-llvm21-ubuntu-24.04:latest-amd64
      options: >-
        --cap-add=PERFMON
        --gpus=all
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12"]
    env:
      DOCC_CUDA_ARCH: sm_120

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.BENCHMARK_TESTS_TOKEN }}
          submodules: recursive

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Create virtual environment
        run: |
          python -m venv .venv
          echo "$PWD/.venv/bin" >> $GITHUB_PATH
          echo "PYTHONPATH=$PWD/python:$PWD/mlir" >> $GITHUB_ENV

      - name: Mark GitHub Actions workdir as safe
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pybind11 coverage build scikit-build-core
          pip install pytest pytest-xdist
          pip install numpy scipy ml_dtypes jsonschema cupy-cuda12x transformers

          pip install -r mlir/requirements-cuda.txt

      - name: Build
        run: |
          mkdir build
          cd build
          cmake -G Ninja \
            -DCMAKE_C_COMPILER=clang-21 \
            -DCMAKE_CXX_COMPILER=clang++-21 \
            -DCMAKE_BUILD_TYPE=Debug \
            -DPYTHON_BUILD_FRONTEND=ON \
            -Dpybind11_DIR=$GITHUB_WORKSPACE/.venv/lib/python${{ matrix.python-version }}/site-packages/pybind11/share/cmake/pybind11 \
            -DMLIR_BUILD_FRONTEND=ON \
            -DMLIR_DIR=/usr/lib/llvm-21/lib/cmake/mlir \
            -DLLVM_EXTERNAL_LIT=/usr/lib/llvm-21/build/utils/lit/lit.py \
            -DINSTALL_GTEST=OFF \
            -DBUILD_TESTS:BOOL=OFF \
            -DBUILD_BENCHMARKS:BOOL=OFF \
            -DBUILD_BENCHMARKS_GOOGLE:BOOL=OFF \
            ..
          ninja -j$(nproc)

      - name: Python & MLIR Tests
        env:
          DOCC_ACCESS_TOKEN: ${{ secrets.DOCC_CI_TOKEN }}
        run: |
          nvidia-smi
          export LD_LIBRARY_PATH=/usr/local/lib:$LD_LIBRARY_PATH

          pytest -v -n auto python/tests/cuda -m "cuda"
          pytest -v python/tests/python/ -m "cuda"
          pytest -v python/tests/sdfg/ -m "cuda"

          pytest -v -n auto python/benchmarks/ -k cuda
          pytest -v -n auto mlir/integration -m "cuda"

          # Segformer correctness test
          pytest -v -n auto mlir/benchmarks/torch/model_zoo/segformer.py -k "cuda"

      - name: PyTorch Tests
        run: |
          python -m venv .venv-pytorch
          .venv-pytorch/bin/python -m pip install --upgrade pip
          .venv-pytorch/bin/pip install -r pytorch/requirements-cuda.txt
          export PYTHONPATH=$PWD/python:$PWD/pytorch

          .venv-pytorch/bin/pytest -v -n auto pytorch/tests --target=cuda

  rocm-tests-linux:
    runs-on:
      group: dahlia
      labels: rocm
    container:
      image: daisytuner/docc-run-env-llvm21-ubuntu-24.04:latest-amd64
      options: >-
        --cap-add=PERFMON
        --device /dev/kfd
        --device /dev/dri
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12"]
    env:
      DOCC_ROCM_ARCH: gfx1201

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.BENCHMARK_TESTS_TOKEN }}
          submodules: recursive

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Create virtual environment
        run: |
          python -m venv .venv
          echo "$PWD/.venv/bin" >> $GITHUB_PATH
          echo "PYTHONPATH=$PWD/python:$PWD/mlir" >> $GITHUB_ENV

      - name: Mark GitHub Actions workdir as safe
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pybind11 coverage build scikit-build-core
          pip install pytest pytest-xdist
          pip install numpy scipy ml_dtypes jsonschema transformers

          pip install -r mlir/requirements-rocm.txt

      - name: Build
        run: |
          mkdir build
          cd build
          cmake -G Ninja \
            -DCMAKE_C_COMPILER=clang-21 \
            -DCMAKE_CXX_COMPILER=clang++-21 \
            -DCMAKE_BUILD_TYPE=Debug \
            -DPYTHON_BUILD_FRONTEND=ON \
            -Dpybind11_DIR=$GITHUB_WORKSPACE/.venv/lib/python${{ matrix.python-version }}/site-packages/pybind11/share/cmake/pybind11 \
            -DMLIR_BUILD_FRONTEND=ON \
            -DMLIR_DIR=/usr/lib/llvm-21/lib/cmake/mlir \
            -DLLVM_EXTERNAL_LIT=/usr/lib/llvm-21/build/utils/lit/lit.py \
            -DINSTALL_GTEST=OFF \
            -DBUILD_TESTS:BOOL=OFF \
            -DBUILD_BENCHMARKS:BOOL=OFF \
            -DBUILD_BENCHMARKS_GOOGLE:BOOL=OFF \
            ..
          ninja -j$(nproc)

      - name: Python & MLIR Tests
        env:
          DOCC_ACCESS_TOKEN: ${{ secrets.DOCC_CI_TOKEN }}
        run: |
          rocm-smi
          export LD_LIBRARY_PATH=/usr/local/lib:$LD_LIBRARY_PATH

          # Make cupy find ROCm libraries
          export ROCM_HOME=/opt/rocm
          export LD_LIBRARY_PATH=/opt/rocm/lib:$LD_LIBRARY_PATH

          pytest -v -n auto python/tests/rocm -m "rocm"
          pytest -v python/tests/python/ -m "rocm"
          pytest -v python/tests/sdfg/ -m "rocm"

          pytest -v -n auto python/benchmarks/ -k rocm
          pytest -v -n auto mlir/integration -m "rocm"

          pytest -v -n auto mlir/benchmarks/torch/model_zoo/segformer.py -k "rocm"

      - name: PyTorch Tests
        run: |
          python -m venv .venv-pytorch
          .venv-pytorch/bin/python -m pip install --upgrade pip
          .venv-pytorch/bin/pip install -r pytorch/requirements-rocm.txt

          export PYTHONPATH=$PWD/python:$PWD/pytorch

          .venv-pytorch/bin/pip install cupy-rocm-7-0

          .venv-pytorch/bin/pytest -v -n auto pytorch/tests --target=rocm

  tenstorrent-tests-linux:
    runs-on:
      group: dahlia
      labels: tenstorrent
    container:
      image: daisytuner/docc-run-env-llvm21-ubuntu-24.04:latest-amd64
      options: >-
        --cap-add=PERFMON
    strategy:
      fail-fast: false
      matrix:
        python-version: [ "3.12" ]

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.BENCHMARK_TESTS_TOKEN }}
          submodules: recursive

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Create virtual environment
        run: |
          python -m venv .venv
          echo "$PWD/.venv/bin" >> $GITHUB_PATH
          echo "PYTHONPATH=$PWD/python:$PWD/mlir" >> $GITHUB_ENV

      - name: Mark GitHub Actions workdir as safe
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pybind11 coverage build scikit-build-core
          pip install pytest pytest-xdist
          pip install numpy scipy ml_dtypes jsonschema
          pip install -r mlir/requirements.txt

      - name: Build
        run: |
          mkdir build
          cd build
          cmake -G Ninja \
            -DCMAKE_C_COMPILER=clang-21 \
            -DCMAKE_CXX_COMPILER=clang++-21 \
            -DCMAKE_BUILD_TYPE=Debug \
            -DPYTHON_BUILD_FRONTEND=ON \
            -Dpybind11_DIR=$GITHUB_WORKSPACE/.venv/lib/python${{ matrix.python-version }}/site-packages/pybind11/share/cmake/pybind11 \
            -DMLIR_BUILD_FRONTEND=ON \
            -DMLIR_DIR=/usr/lib/llvm-21/lib/cmake/mlir \
            -DLLVM_EXTERNAL_LIT=/usr/lib/llvm-21/build/utils/lit/lit.py \
            -DINSTALL_GTEST=OFF \
            -DBUILD_TESTS:BOOL=OFF \
            -DBUILD_BENCHMARKS:BOOL=OFF \
            -DBUILD_BENCHMARKS_GOOGLE:BOOL=OFF \
            ..
          ninja -j$(nproc)

      - name: Python & MLIR Tests
        env:
          DOCC_ACCESS_TOKEN: ${{ secrets.DOCC_CI_TOKEN }}
        run: |
          pytest -v python/tests mlir/integration -m "tenstorrent"

  finish:
    needs: [cpp-tests-linux, python-tests-linux, mlir-tests-linux, llvm-tests-linux, pytorch-tests-linux]
    runs-on: ubuntu-latest
    steps:
      - name: Coveralls Finished
        uses: coverallsapp/github-action@v2
        with:
          parallel-finished: true
